# test-check command

The test-check command reports a configured success or failure to Kuberhealthy
after a configurable delay. It is intended for validating that the Kuberhealthy
reporting pipeline is wired correctly.
