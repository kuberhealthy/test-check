---
apiVersion: kuberhealthy.github.io/v2
kind: HealthCheck
metadata:
  name: test-check
  namespace: kuberhealthy
spec:
  runInterval: 5m
  timeout: 5m
  podSpec:
    spec:
      containers:
        - name: test-check
          image: ghcr.io/kuberhealthy/test-check:v0.1.0
          imagePullPolicy: IfNotPresent
          env:
            - name: REPORT_FAILURE
              value: "false"
            - name: REPORT_DELAY
              value: "5s"
      restartPolicy: Never
